extends Node

var _language = 0; # 0 = English, 1 = Arabic
var _textMenuLabel = ["Menu","القائمة"];
var _textPlayButton = ["Play","أبدا"];
var _textSettingsButton = ["Settings","الإعدادات"];
var _textExitButton = ["Exit","خروج"]
var _textLanguageLabel = ["Language:",":اللغة"]


# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
